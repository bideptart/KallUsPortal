node_modules
dist

# Secrets — never commit. Use .env.example as the template.
.env
.env.local
.env.*.local

# SSH keys — never commit
ssh
ssh.pub
*.pem
id_rsa*

.DS_Store
*.log
nohup.out
myenv/
__pycache__/
logs/
log/

# Runtime caches
server/tts-cache/

# Local SQLite demo-login DB — regenerated (and reseeded) automatically on
# first server start, see server/sqliteAuth.js.
server/data/
